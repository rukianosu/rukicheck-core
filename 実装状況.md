# RukiCheck 実装状況

最終更新: 2025-12-23

## 📋 目次

1. [実装済み機能](#実装済み機能)
2. [未実装機能](#未実装機能)
3. [機能仕様書](#機能仕様書)
4. [今後の改善提案](#今後の改善提案)

---

## ✅ 実装済み機能

### 基本検査機能

#### 💾 ストレージ検査
- [x] Cドライブの空き容量チェック
- [x] 総容量・使用量・空き容量の取得
- [x] 空き容量に応じた自動判定（danger/warn/ok）
- [x] 空き容量が少ない場合の推奨アクション表示

#### ⌨️ キーボード検査
- [x] JIS/USレイアウト自動判定
- [x] 全キーの入力検知
- [x] 押下済みキーのリアルタイム表示
- [x] 未押下キーの検出と一覧表示
- [x] 自動判定（全キー押下で「ok」、欠けがあれば「warn」）

#### 🎤 マイク検査
- [x] 5秒間の音声録音
- [x] 録音した音声の再生確認
- [x] WAVファイルの保存（attachments/mic_test.wav）
- [x] ユーザーによる音声確認（はい/いいえ）

#### 🔊 スピーカー検査
- [x] 左チャンネルテスト音再生（440Hz、2秒間）
- [x] 右チャンネルテスト音再生（440Hz、2秒間）
- [x] **モノラル/ステレオデバイス自動判定**
- [x] **スムーズなパンニングテスト（左→右へ5秒間）**
- [x] ユーザーによる音声確認（はい/いいえ）

#### 📷 カメラ検査
- [x] カメラデバイスの自動検出
- [x] プレビュー表示
- [x] 静止画撮影
- [x] JPG画像の保存（attachments/camera_test.jpg）

#### 🖥️ CPU検査
- [x] 60秒間の負荷テスト
- [x] フリーズ/クラッシュ検知
- [x] テスト時間の計測
- [x] 異常検知時の警告

#### 🖱️ トラックパッド検査
- [x] カーソル移動の検知
- [x] スクロール動作の検知
- [x] クリック検知（左/右ボタン）
- [x] 自動判定（移動とスクロール両方確認で「ok」）

### ハードウェア情報収集

#### 💻 システム情報
- [x] **PCメーカー名の取得（Win32_ComputerSystem）**
- [x] **PC型番の取得（Win32_ComputerSystem）**
- [x] **シリアルナンバーの取得（Win32_BIOS）**
- [x] **BIOSバージョンの取得**
- [x] OEMプレースホルダー値の適切な処理

#### 💾 ストレージ詳細情報
- [x] ドライブタイプ（HDD/SSD/NVMe）の判定
- [x] 総容量・空き容量の取得
- [x] ストレージヘルス情報（MSFT_StorageReliabilityCounter）
- [x] 使用時間（PowerOnHours）の取得
- [x] SMART情報の一部取得

#### 🧠 メモリ情報
- [x] 物理メモリ総容量の取得
- [x] メモリモジュール個別情報（メーカー、容量、速度）
- [x] メモリタイプの取得（DDR3/DDR4/DDR5）
- [x] メモリスロット数の取得

#### 🎨 GPU情報
- [x] GPU名の取得
- [x] VRAMサイズの取得

#### 🔐 BitLocker情報
- [x] Cドライブの暗号化状態取得
- [x] 暗号化方式の取得
- [x] 保護状態の取得

### レポート機能

#### 📄 レポート生成
- [x] **JSON形式レポート（report.json）**
- [x] **HTML形式レポート（report.html）**
- [x] **Markdown形式レポート（report.md）**
- [x] レポートフォルダの自動生成
- [x] 添付ファイル（音声、画像）の保存

#### 📊 レポート内容
- [x] 検品メタ情報（管理番号、日時、ツールバージョン）
- [x] ハードウェア詳細情報
- [x] システム情報（メーカー、型番、シリアル）
- [x] BitLocker情報
- [x] ストレージヘルス情報
- [x] 全検査項目の結果

### UI/UX機能

#### 🎯 メインウィンドウ
- [x] 管理番号入力フィールド
- [x] 出力フォルダ選択ダイアログ
- [x] 検品開始ボタン
- [x] **自動検査ボタン（全検査を順番に実行）**
- [x] レポート保存ボタン
- [x] ステータスバー（進捗表示）
- [x] 各検査項目の個別実行ボタン

#### 🪟 検査ウィンドウ
- [x] キーボード検査ウィンドウ
- [x] マイク検査ウィンドウ
- [x] スピーカー検査ウィンドウ（左/右/パンニングテスト）
- [x] カメラ検査ウィンドウ
- [x] トラックパッド検査ウィンドウ

#### 🔄 自動検査機能
- [x] 全検査の順次自動実行
- [x] 進捗表示（1/7、2/7...）
- [x] エラーハンドリング
- [x] 完了通知ダイアログ

### アーキテクチャ

#### 🏗️ 設計パターン
- [x] MVVMパターンの採用
- [x] 依存性注入（DI）の実装
- [x] サービス層の分離
- [x] 非同期処理（async/await）
- [x] INotifyPropertyChangedの実装

#### 📦 ビルド設定
- [x] .NET 8 WPFアプリケーション
- [x] PublishSingleFile（単一EXE）
- [x] SelfContained（ランタイム内蔵）
- [x] 標準ユーザー権限で実行可能

---

## ❌ 未実装機能

### ハードウェア診断

#### 💾 ストレージ詳細診断
- [ ] SMART完全情報の取得
- [ ] NVMe専用ヘルス情報
- [ ] SSD寿命予測（TBW）
- [ ] 不良セクタ数の取得
- [ ] ディスク速度ベンチマーク

#### 🌡️ 温度・ファン監視
- [ ] CPU温度の取得（WMIでは困難）
- [ ] GPU温度の取得
- [ ] ファン回転数の取得
- [ ] サーマルスロットリング検知

#### 🔐 BitLocker拡張
- [ ] BitLocker回復キーの取得（管理者権限が必要）
- [ ] TPMバージョン情報
- [ ] セキュアブート状態

#### 🧠 メモリテスト
- [ ] メモリ完全テスト（エラーチェック）
- [ ] メモリ速度ベンチマーク
- [ ] デュアルチャンネル動作確認

#### 🔌 その他ハードウェア
- [ ] バッテリーヘルス（ノートPC）
- [ ] ディスプレイ情報（解像度、リフレッシュレート）
- [ ] ネットワークアダプター情報
- [ ] Bluetoothデバイス情報

### レポート機能

#### 📄 出力形式
- [ ] PDF形式レポート
- [ ] Excel形式レポート
- [ ] CSVエクスポート

#### 📊 レポート内容
- [ ] グラフ・チャート表示
- [ ] 前回検査との比較
- [ ] 履歴トレンド表示

### UI/UX機能

#### 🎨 見た目
- [ ] テーマ切り替え（ダーク/ライトモード）
- [ ] 多言語対応（英語、中国語など）
- [ ] カスタマイズ可能なレイアウト

#### ⚙️ 設定機能
- [ ] 検査項目のカスタマイズ
- [ ] 判定基準の変更
- [ ] デフォルト出力先の設定

### その他

#### 🚀 起動オプション
- [ ] USBブート機能
- [ ] コマンドライン引数対応
- [ ] サイレントモード（自動実行）

#### 🔄 更新機能
- [ ] 自動アップデート機能
- [ ] バージョンチェック

---

## 📖 機能仕様書

### 検査項目仕様

#### 1. ストレージ検査

**目的**: Cドライブの空き容量を確認し、容量不足を警告する

**実行方法**: 自動（WMIクエリ）

**判定基準**:
- `danger`: 空き容量 < 10% または < 10GB
- `warn`: 空き容量 < 20% または < 20GB
- `ok`: 上記以外

**出力データ**:
```json
{
  "drive": "C:",
  "total_gb": 476.9,
  "free_gb": 18.2,
  "free_percent": 3.8,
  "status": "danger",
  "recommendation": "SSD容量アップ推奨（例：1TB）"
}
```

---

#### 2. キーボード検査

**目的**: 全キーの入力を確認し、故障キーを検出する

**実行方法**: ユーザーが全キーを押下

**判定基準**:
- `ok`: 全キーが押下された
- `warn`: 一部のキーが押下されていない

**機能**:
- JIS/USレイアウト自動判定（109キー/104キー）
- 押下済みキーをリアルタイム表示（青色）
- 未押下キーを赤色で強調表示
- 完了ボタンは全キー押下後に有効化

**出力データ**:
```json
{
  "layout": "JIS",
  "total_keys": 109,
  "pressed_keys": 108,
  "missing_keys": ["F12"],
  "result": "warn"
}
```

---

#### 3. マイク検査

**目的**: マイクが正常に録音できるか確認する

**実行方法**:
1. ユーザーが録音ボタンをクリック
2. 5秒間録音
3. 録音した音声を再生
4. ユーザーが「聞こえた」「聞こえなかった」を選択

**判定基準**:
- ユーザーの主観判定（はい/いいえ）

**保存データ**:
- `attachments/mic_test.wav` (5秒、16kHz、モノラル)

**出力データ**:
```json
{
  "recording_duration_sec": 5,
  "file_path": "attachments/mic_test.wav",
  "user_confirmed": true
}
```

---

#### 4. スピーカー検査

**目的**: 左右スピーカーが正常に音を出力できるか確認する

**実行方法**:
1. 左スピーカーから440Hzのテスト音を2秒間再生
2. ユーザーが「聞こえた」「聞こえなかった」を選択
3. 右スピーカーから440Hzのテスト音を2秒間再生
4. ユーザーが「聞こえた」「聞こえなかった」を選択
5. （オプション）パンニングテスト: 5秒間で左から右へスムーズに音が移動

**判定基準**:
- ユーザーの主観判定（はい/いいえ）

**技術仕様**:
- 周波数: 440Hz（A4音）
- 音量: Gain 0.2（音割れ防止）
- デバイス自動判定: ステレオ/モノラル
  - ステレオデバイス: PanningSampleProvider使用
  - モノラルデバイス: StereoToMonoSampleProvider使用
- パンニング: -1.0（左）→ +1.0（右）へ50msごとに更新

**出力データ**:
```json
{
  "left_confirmed": true,
  "right_confirmed": true
}
```

---

#### 5. カメラ検査

**目的**: カメラが正常に動作するか確認する

**実行方法**:
1. プレビューウィンドウを表示
2. ユーザーが撮影ボタンをクリック
3. 静止画を撮影してJPG保存

**判定基準**:
- `ok`: 撮影成功
- `error`: カメラ未検出またはエラー

**保存データ**:
- `attachments/camera_test.jpg` (640x480または可能な最大解像度)

**出力データ**:
```json
{
  "camera_detected": true,
  "image_path": "attachments/camera_test.jpg",
  "result": "ok"
}
```

---

#### 6. CPU検査

**目的**: CPUが高負荷時に正常動作するか確認する

**実行方法**:
1. 60秒間、全コアで並列計算を実行
2. 応答性を監視（フリーズ検知）
3. クラッシュやエラーを検知

**判定基準**:
- `ok`: 60秒間正常動作、異常なし
- `error`: フリーズ、クラッシュ、エラー発生

**技術仕様**:
- 負荷内容: 素数判定計算（`IsPrime()`）を繰り返し実行
- タイムアウト: 60秒

**出力データ**:
```json
{
  "stress_test": true,
  "duration_sec": 60,
  "abnormal": false
}
```

---

#### 7. トラックパッド検査

**目的**: トラックパッドのカーソル移動、スクロール、クリックを確認する

**実行方法**:
1. ユーザーがカーソルを移動
2. ユーザーがスクロール操作
3. ユーザーが左/右クリック
4. 完了ボタンをクリック

**判定基準**:
- `ok`: カーソル移動とスクロール両方確認
- `warn`: どちらかが確認できない

**出力データ**:
```json
{
  "cursor_moved": true,
  "scroll_detected": true,
  "left_click": true,
  "right_click": true,
  "result": "ok"
}
```

---

#### 8. 自動検査機能

**目的**: 全検査を自動で順次実行し、検査漏れを防ぐ

**実行方法**:
1. 「🔄 自動検査開始」ボタンをクリック
2. 以下の順序で自動実行:
   1. ストレージ検査
   2. キーボード検査
   3. マイク検査
   4. スピーカー検査
   5. カメラ検査
   6. トラックパッド検査
   7. CPU検査

**進捗表示**:
- ステータスバーに「1/7: ストレージ検査中...」「2/7: キーボード検査中...」と表示
- 各検査の間に0.5秒の待機時間

**完了時の動作**:
- 「✅ 全ての検査が完了しました！」メッセージ表示
- ダイアログで「レポート保存」を促す

---

### ハードウェア情報取得仕様

#### システム情報（Win32_ComputerSystem / Win32_BIOS）

**取得項目**:
- メーカー名（Manufacturer）
- 型番（Model）
- シリアルナンバー（SerialNumber）
- BIOSバージョン（SMBIOSBIOSVersion）

**特殊処理**:
- "To Be Filled By O.E.M." → "取得不可"
- "Default string" → "取得不可"
- null/空文字 → "不明"

**出力例**:
```json
{
  "manufacturer": "Dell Inc.",
  "model": "Latitude 7490",
  "serial_number": "ABCD1234",
  "bios_version": "1.15.0"
}
```

---

#### ストレージヘルス情報（MSFT_StorageReliabilityCounter）

**取得項目**:
- PowerOnHours（使用時間）
- ReadErrorsTotal（読み取りエラー数）
- WriteErrorsTotal（書き込みエラー数）
- Temperature（温度）

**出力例**:
```json
{
  "health": {
    "power_on_hours": 12345,
    "read_errors": 0,
    "write_errors": 0,
    "temperature_celsius": 35
  }
}
```

---

#### メモリ情報（Win32_PhysicalMemory）

**取得項目**:
- メーカー（Manufacturer）
- 容量（Capacity）
- 速度（Speed）
- メモリタイプ（MemoryType / SMBIOSMemoryType）

**DDRタイプ判定**:
- `24` → DDR3
- `26` → DDR4
- `34` → DDR5
- その他 → Unknown

**出力例**:
```json
{
  "modules": [
    {
      "manufacturer": "Samsung",
      "capacity_gb": 8,
      "speed_mhz": 2400,
      "type": "DDR4"
    }
  ]
}
```

---

#### BitLocker情報（Win32_EncryptableVolume）

**取得項目**:
- 暗号化状態（ProtectionStatus）
- 暗号化方式（EncryptionMethod）

**暗号化方式マッピング**:
- `0` → なし
- `1` → AES 128
- `2` → AES 256
- `3` → AES 128 Diffuser
- `4` → AES 256 Diffuser
- `6` → XTS-AES 128
- `7` → XTS-AES 256

**出力例**:
```json
{
  "drive": "C:",
  "encrypted": true,
  "encryption_method": "XTS-AES 256",
  "protection_status": 1
}
```

---

### レポート仕様

#### JSON形式（report.json）

**用途**: プログラムで読み取る正データ

**特徴**:
- 構造化データ
- 完全な検査結果を含む
- UTF-8エンコーディング

#### HTML形式（report.html）

**用途**: お客様提出用レポート

**特徴**:
- ブラウザで表示可能
- 視覚的に分かりやすいデザイン
- カラーコード（緑/黄/赤）で結果を強調
- テーブル形式で情報を整理
- 埋め込みCSS（スタンドアロンHTML）

#### Markdown形式（report.md）

**用途**: GitHub/テキストエディタで表示

**特徴**:
- プレーンテキストベース
- Markdownテーブルで構造化
- 絵文字アイコンで視認性向上
- バージョン管理に適している

---

## 💡 今後の改善提案

### 優先度: 高 🔴

#### 1. DDRメモリタイプ検出の改善

**現状の問題**:
- 一部のPCでDDRタイプが「Unknown」と表示される
- WMI経由では正確な情報が取れない場合がある

**改善案**:
- CPU-Zのようなハードウェアレジスタ直接読み取り
- SPD情報の取得（I2C経由）
- Windows RegistryからSMBIOS情報を取得

**優先度**: 高（ユーザーからの要望が多い）

---

#### 2. VRAMサイズ表示の改善

**現状の問題**:
- HTMLレポートでVRAMが表示されている
- JSONには含まれているが、見た目が冗長

**改善案**:
- HTMLからVRAM表示を削除（JSONのみに記録）
- または、オプションで表示/非表示を切り替え

**優先度**: 中（見た目の改善）

---

### 優先度: 中 🟡

#### 3. エラーハンドリングの強化

**改善案**:
- カメラ/マイクのプライバシー設定エラーを詳細に表示
- WMI取得失敗時のフォールバック処理
- エラーログの保存

**実装方法**:
- `try-catch` ブロックでエラーキャッチ
- エラー詳細をステータスメッセージに表示
- `error_log.txt` に記録

---

#### 4. レポートのカスタマイズ機能

**改善案**:
- 検査項目の選択（必要な項目のみ実行）
- 判定基準のカスタマイズ（例: ストレージ空き容量の閾値変更）
- 会社ロゴの挿入（HTMLレポート）

**実装方法**:
- 設定ファイル（`config.json`）の導入
- 設定画面の追加

---

#### 5. 履歴機能

**改善案**:
- 同じPCの過去の検査結果を表示
- 前回検査との比較表示
- トレンドグラフ（ストレージ空き容量の推移など）

**実装方法**:
- シリアルナンバーをキーにして履歴を保存
- SQLite データベースまたはJSON履歴ファイル

---

### 優先度: 低 🟢

#### 6. テーマ切り替え

**改善案**:
- ダークモード/ライトモードの切り替え
- カラースキームのカスタマイズ

**実装方法**:
- WPFのResourceDictionaryでテーマ定義
- 設定で切り替え

---

#### 7. 多言語対応

**改善案**:
- 英語、中国語などの多言語対応
- UIメッセージの翻訳

**実装方法**:
- リソースファイル（`.resx`）で翻訳管理
- 言語設定の追加

---

#### 8. PDF形式レポート

**改善案**:
- PDFレポートの生成
- 印刷に適したフォーマット

**実装方法**:
- iTextSharp または PdfSharp ライブラリ使用
- HTMLをPDFに変換

---

### 技術的改善

#### 9. ユニットテストの追加

**現状**: テストコードが存在しない

**改善案**:
- xUnit または NUnit でテストプロジェクト作成
- サービス層のユニットテスト
- モックを使った依存関係の分離

---

#### 10. CIパイプラインの構築

**改善案**:
- GitHub Actionsで自動ビルド
- 自動テスト実行
- リリースビルドの自動生成

**実装方法**:
```yaml
# .github/workflows/build.yml
name: Build and Test
on: [push, pull_request]
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 8.0.x
      - name: Restore
        run: dotnet restore
      - name: Build
        run: dotnet build -c Release
      - name: Test
        run: dotnet test
```

---

#### 11. コード品質の向上

**改善案**:
- StyleCop または .editorconfig でコーディング規約統一
- SonarQube でコード品質分析
- コメント・ドキュメントの充実

---

### パフォーマンス改善

#### 12. 起動時間の最適化

**現状**: 初回起動が遅い可能性

**改善案**:
- 遅延初期化（Lazy Initialization）
- 必要なサービスのみ起動時に読み込み

---

#### 13. WMIクエリの並列化

**改善案**:
- 複数のWMIクエリを並列実行
- `Task.WhenAll()` で待機時間短縮

**実装例**:
```csharp
var systemInfoTask = Task.Run(() => GetSystemInfo());
var memoryInfoTask = Task.Run(() => GetMemoryInfo());
var storageInfoTask = Task.Run(() => GetStorageInfo());

await Task.WhenAll(systemInfoTask, memoryInfoTask, storageInfoTask);
```

---

## 📊 機能実装率

| カテゴリ | 実装済み | 未実装 | 実装率 |
|---------|---------|-------|-------|
| 基本検査 | 7/7 | 0/7 | 100% |
| ハードウェア情報 | 6/10 | 4/10 | 60% |
| レポート機能 | 3/5 | 2/5 | 60% |
| UI/UX | 8/11 | 3/11 | 73% |
| **合計** | **24/33** | **9/33** | **73%** |

---

## 🎯 次のマイルストーン

### v0.2 リリース予定機能

1. ✅ スムーズなパンニング音声テスト ← **完了**
2. ✅ 自動検査機能 ← **完了**
3. ✅ Markdownレポート生成 ← **完了**
4. ⏳ DDRメモリタイプ検出の改善
5. ⏳ エラーハンドリングの強化
6. ⏳ 実機5台以上でのテスト

### v1.0 リリース条件

- [ ] 全検査項目が安定動作
- [ ] 実機10台以上でテスト完走
- [ ] ドキュメント整備（使い方ガイド、FAQ）
- [ ] CI/CD パイプライン構築
- [ ] ユニットテストカバレッジ 80%以上

---

**最終更新**: 2025-12-23
**作成者**: Claude Code
**プロジェクト**: RukiCheck v0.1.0
